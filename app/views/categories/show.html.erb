<p id="notice"><%= notice %></p>

   <% if current_user.admin?  %>
 </br></br></br></br></br>
   <p>
  <strong>Category name:</strong>
  <%= @category.category_name %>
</p>
<ul >
         
     </br>
 
    <% get_books.each do |book| %>
   <li > 
   <%= link_to 'Show', book %> <%= book.name %> 
   <%= book.price %> </li>  
    
    <% end %>
   </ul>
<%= link_to 'Edit', edit_category_path(@category) %> |
<%= link_to 'Back', categories_path %>
 
<%else%>
 <body class="body2">

    

    <div class="container-fluid">
      <div class="row" >
        <div class="col-sm-3 col-md-2 sidebar">
          <ul class="nav nav-sidebar">
           
      <%  @categoriess= Category.all %>
       <% @categoriess.each do |category| %>
      
        <li><%= link_to category.category_name, category %></li>
     
    <% end %>
          </ul>
     
          
        </div>
         <!-- Placed at the end of the document so the pages load faster -->
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/1.11.3/jquery.min.js"></script>
    <script src="../../dist/js/bootstrap.min.js"></script>
    <!-- Just to make our placeholder images work. Don't actually copy the next line! -->
    <script src="../../assets/js/vendor/holder.min.js"></script>
    <!-- IE10 viewport hack for Surface/desktop Windows 8 bug -->
    <script src="../../assets/js/ie10-viewport-bug-workaround.js"></script>
  </body>

  
  <ul style="margin-left:300px; margin-top:200px">
          <em><h1 style="margin-left:300px; margin-top:-150px;color:#5a7032">
   <%= @category.category_name %></h1></em>
     </br>
 
    <% get_books.each do |book| %>
   <li class="list"> 
    <%= link_to (image_tag book.image),book%>

     <img src="/assets/<%=book.image%> " width="200" height="200"/> </br> <%= link_to book.name, book %></br><%= number_to_currency book.price , unit: "£"%> <%= render 'shopping_carts/form', :book_id => book.id, :a =>0 %>  </li>  
   
    <% end %>
   </ul>
    <%end%>