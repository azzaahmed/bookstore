
<%= form_tag "/shopping_carts/adddd",:method => "post" do %>

  <div class="field" >
<%= label_tag :quantity, "Quantity:" %> 
 
<%= number_field_tag :quantity, '1', min: 1 %>

<%= hidden_field_tag "book_id", book_id %>

<% if a===0 %>
<%= submit_tag "Add to cart" %>
<% else %>
<%= submit_tag "add" %>

<%= submit_tag "remove" %>
<%end%>
</div>
<% end %>