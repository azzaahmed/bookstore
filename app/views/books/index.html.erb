<p id="notice"><%= notice %></p>


 <% if @books.present? %>
 

<h1>Listing Books</h1>
 <% if can? :new, @book %>
<%= link_to 'New Book', new_book_path %>
<% end %>

<table>
  <thead>
    <tr>
      <th>Price</th>
  
      <th>Name</th>
    
     <!--  <th>Image</th> -->
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody >
    <% @books.each do |book| %>
      <tr>
        <td> <%= number_to_currency book.price , unit: "£"%></td>
       
        <td><%= book.name %></td>
      
      
<!--         <td><%= book.image %></td> -->
      <td>   <%= image_tag book.imagee.url, :size => '100x100' %> </td>

    <!--  Add to cart -->
<td>  <%= render 'shopping_carts/form', :book_id => book.id , :a =>0 %> </td>
      
        <td><%= link_to 'Show', book %></td>  
        <% if can? :new, @book %>
        <td><%= link_to 'Edit', edit_book_path(book) %></td>
        <td><%= link_to 'Destroy', book, method: :delete, data: { confirm: 'Are you sure?' } %></td>

        <% end %>

      </tr>
    <% end %>
  </tbody>
</table>

<br>

<% else %>
  <p class="alert alert-danger" role="alert">There are no book name containing the term(s) <%= params[:search] %>.</p>
<% end %>
 