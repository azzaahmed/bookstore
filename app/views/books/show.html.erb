<p id="notice"><%= notice %></p>
<% if ! current_user.admin?  %>
<p>
  <strong>Price:</strong>
   <%= number_to_currency @book.price , unit: "£"%>
</p>

<p>
  <strong>Description:</strong>
  <%= @book.description %>
</p>

<p>
  <strong>Name:</strong>
  <%= @book.name %>
</p>

<p>
  <strong>Author:</strong>
  <%= @book.author %>
</p>

<p>
  <strong>Category:</strong>
  <%= @book.category_id %>
</p>

<p>
  <strong>Image:</strong>
  <%= @book.image %>
  
  
  <img src="/assets/<%=@book.image%>" />
<!--    <%= link_to (image_tag @book.image),@book%> -->
<!--    <img src= <%= image_path(@book.image)%> />
 <img src=<%=image_path("point blanc.jpg")%> />
 
 <%= image_tag (@book.image) %>
 <%= image_tag @book.image %> -->

</p>
<% if can? :edit, @book %>
<%= link_to 'Edit', edit_book_path(@book) %> 
<% end %>
<% @category=Category.find(@book.category_id) %>
<%= link_to 'Back', @category %>

<%else%>
</br></br></br></br></br>
<p>
  <strong>Price:</strong>
   <%= number_to_currency @book.price , unit: "£"%>
</p>

<p>
  <strong>Description:</strong>
  <%= @book.description %>
</p>

<p>
  <strong>Name:</strong>
  <%= @book.name %>
</p>

<p>
  <strong>Author:</strong>
  <%= @book.author %>
</p>

<p>
  <strong>Category:</strong>
  <%= @book.category_id %>
</p>

<p>
  <strong>Image:</strong>
  <%= @book.image %>

</p>

<%= link_to 'Edit', edit_book_path(@book) %> 

<% @category=Category.find(@book.category_id) %>
<%= link_to 'Back', @category %>

<%end%>